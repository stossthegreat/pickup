name: Build Flutter AAB - Final

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.workers.max=2"
      JAVA_TOOL_OPTIONS: "-Xmx3g"

    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: 'gradle'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.6'
          channel: stable
          cache: true

      - name: Write local.properties
        run: |
          mkdir -p android
          FLUTTER_SDK=$(dirname "$(dirname "$(which flutter)")")
          echo "flutter.sdk=$FLUTTER_SDK" > android/local.properties
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> android/local.properties

      - name: Accept Android SDK licenses
        run: yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses

      - name: Precache Flutter engine
        run: flutter precache --android --force

      - name: flutter pub get
        run: flutter pub get

      - name: Verify keystore
        run: |
          set -e
          echo "================================================================"
          echo " EXPECTED FINGERPRINTS (registered with Play Console for Mirrorly)"
          echo "================================================================"
          echo "         SHA1: C2:27:D7:BE:A2:38:70:40:16:6A:E3:D9:BD:23:39:8D:60:DC:08:DE"
          echo "         SHA256: 76:E1:49:7A:35:36:DD:9A:02:8C:DB:46:5F:F8:D2:38:18:C0:FC:40:A2:55:53:C5:C7:AB:CF:2B:A7:5C:BD:ED"
          echo "================================================================"
          echo ""
          echo "Using keystore from repo: android/app/upload-keystore.jks"
          ls -l android/app/upload-keystore.jks
          echo ""
          echo "Actual fingerprints from keystore:"
          keytool -list -v -keystore android/app/upload-keystore.jks -alias skeletalpt -storepass skeletalpt123 -keypass skeletalpt123 | grep -E "SHA1:|SHA256:"
          echo "KEY_ALIAS=skeletalpt" >> $GITHUB_ENV
          echo "STORE_PASSWORD=skeletalpt123" >> $GITHUB_ENV
          echo "KEY_PASSWORD=skeletalpt123" >> $GITHUB_ENV

      - name: Build signed AAB
        run: |
          flutter build appbundle --release --build-number $GITHUB_RUN_NUMBER --no-tree-shake-icons

      - name: Upload artifact (primary path)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: build/app/outputs/bundle/release/app-release.aab
          if-no-files-found: warn

      - name: Upload artifact (fallback path)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab-fallback
          path: build/app/outputs/bundle/*/app-*.aab
          if-no-files-found: ignore
