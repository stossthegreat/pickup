{
  "name": "pickup-monorepo",
  "private": true,
  "description": "Pickup monorepo root — Railway deploys the unified backend (backend) from here",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "install-backend": "cd backend && npm install --omit=dev",
    "postinstall": "cd backend && npm install --omit=dev"
  }
}
