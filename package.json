{
  "name": "pickup-monorepo",
  "private": true,
  "description": "Pickup monorepo root — Railway deploys the unified backend (backend2) from here",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "cd backend2 && npm start",
    "install-backend": "cd backend2 && npm install --omit=dev",
    "postinstall": "cd backend2 && npm install --omit=dev"
  }
}
