# Copy to .env locally for `npm run dev`, or paste straight into Railway
# Project → Variables. Only one variable is required.

OPENAI_API_KEY=sk-...

# Optional — Railway sets PORT automatically; override only for local dev.
PORT=8080

# Optional — bump to "debug" for verbose request logging.
LOG_LEVEL=info
