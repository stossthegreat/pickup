# Scan tab — hero split image

| File | Used by | Brief |
|---|---|---|
| `optimised_split.jpg` | Scan tab Current vs Optimised hero | Single image, vertical split: left half labelled CURRENT (raw face), right half labelled OPTIMISED (same face rendered at full strength). |

Format: JPEG, 4:5 portrait, 1600 × 2000 px, sRGB, ~85% quality, ≤ 500 KB.

This is the "what could you become" hook — must read as the SAME face,
clearly improved, not a different model. Subtle changes: cleaner skin,
defined jaw, fuller hair, set eyes. No plastic-surgery exaggeration.
