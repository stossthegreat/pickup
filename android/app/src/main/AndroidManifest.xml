<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required by flutter_local_notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets the streak nudge survive a device reboot without needing the
         user to open the app first. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Google Play Billing — required for purchases_flutter / RevenueCat to
         talk to the Play Store IAP service. The purchases_flutter plugin's
         manifest declares this, but we declare it explicitly here so a
         silent Gradle manifest-merge failure can't leave us without the
         permission. Without it: paywall buttons appear to work but no
         purchase sheet ever opens on Android. -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- Auralay graft: Eyes voice drills + Game tab voice surfaces.
         RECORD_AUDIO is requested at runtime; MODIFY_AUDIO_SETTINGS lets
         the `record` plugin force the speakerphone on so playback isn't
         routed to the earpiece during a drill; WAKE_LOCK keeps the
         screen alive during sessions (consumed by wakelock_plus);
         FOREGROUND_SERVICE / FOREGROUND_SERVICE_MICROPHONE are required
         by record_android 1.5+ on Android 14+ so a backgrounded drill
         doesn't get killed mid-recording. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <application
        android:label="Mirrorly"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Play Console upload-key fingerprints, embedded in the AAB. -->
        <meta-data
            android:name="com.mirrorly.PLAY_UPLOAD_KEY_SHA1"
            android:value="C2:27:D7:BE:A2:38:70:40:16:6A:E3:D9:BD:23:39:8D:60:DC:08:DE" />
        <meta-data
            android:name="com.mirrorly.PLAY_UPLOAD_KEY_SHA256"
            android:value="76:E1:49:7A:35:36:DD:9A:02:8C:DB:46:5F:F8:D2:38:18:C0:FC:40:A2:55:53:C5:C7:AB:CF:2B:A7:5C:BD:ED" />
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
